import { AsteriscosPipe } from './asteriscos.pipe';

describe('AsteriscosPipe', () => {
  it('create an instance', () => {
    const pipe = new AsteriscosPipe();
    expect(pipe).toBeTruthy();
  });
});
